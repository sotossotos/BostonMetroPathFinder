/**
 * Interactive route finding app for underground metroes.
 *
 * Accepts a file describing the underground metro map as a command line
 * argument, parses the file, asks the user to select an origin and destination
 * stations  and finds the shortest route with less lane changes.
 */
package MetroSystem;
